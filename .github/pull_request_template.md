## What & why

<!-- What does this change, and why? -->

## Checklist

- [ ] `make test` passes
- [ ] `pre-commit run --all-files` passes (or `pre-commit install` is active)
- [ ] `charts/uptime-kuma-operator/crds/` regenerated via `make manifests` if `api/v1alpha1` changed
- [ ] `docs/README.md` / chart README updated if user-facing behavior changed
